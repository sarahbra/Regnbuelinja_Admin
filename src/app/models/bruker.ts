export class Bruker {
    brukernavn: string
    passord: string

    constructor(brukernavn: string, passord: string) {
        this.brukernavn = brukernavn;
        this.passord = passord;
    }
}

